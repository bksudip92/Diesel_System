/**
 * @deprecated Use useAuth() from '@/context/AuthProvider' instead.
 * Import: import { useAuth } from '@/context/AuthProvider';
 * 
 * AuthProvider provides: { session, profile, loading, signOut }
 */
export { useAuth } from '@/context/AuthProvider';

